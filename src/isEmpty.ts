/**
 * Что было плохо: Логическое отрицание перед вызовом метода ухудшало читаемость кода.
 * Что стало лучше: Код стал более простым и понятным благодаря использованию позитивного условия.
 */

if (hasItems(list)) {
  // код, если список не пуст
}
