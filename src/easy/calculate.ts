// Плохое именование + отсутствие валидации
const calculate = (number: number) => {
  return number * number;
};

// Отсутствие типизации + плохое именование
function calculate2(a, b) {
  return a + b;
}
